[project]
name = "world-cup-2026-predictor"
version = "0.1.0"
description = "World Cup 2026 match prediction, simulation, and dashboard project."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
